is_non_player_tag = {
	custom_trigger_tooltip = {
		tooltip = is_non_player_tag_tt
		OR = {
			tag = TES
			tag = EUR
			tag = ENG
			tag = FRA
			tag = SPR
			tag = GER
			tag = ITA
			tag = AUS
			tag = DEN
			tag = SWE
			tag = RUS
			tag = USA
			tag = CSA
			tag = BRA
			tag = HOL
		}
	}
}

is_player_tag = {
	NOT = {
		OR = {
			tag = EUR
			tag = TES
		}
	}
}

is_possible_colonizer = {
	custom_trigger_tooltip = {
		tooltip = is_possible_colonizer_tt
		OR = {
			tag = ENG
			tag = FRA
			tag = SPR
			tag = POR
			tag = GER
			tag = ITA
			tag = AUS
			tag = DEN
			tag = SWE
			tag = RUS
			tag = USA
			tag = CSA
			tag = BRA
			tag = HOL
			tag = BEL
		}
	}
}

is_great_power_eligible = {
	# custom_trigger_tooltip = {
	# 	tooltip = is_great_power_eligible_tt
		is_puppet = no
		OR = {
			is_possible_colonizer = yes
			check_variable = { max_manpower_k > 25000 }
			AND = {
				check_variable = { max_manpower_k > 20000 }
				check_variable = {  num_owned_controlled_states > 10 }
			}
			AND = {
				check_variable = { max_manpower_k > 10000 }
				check_variable = {  num_owned_controlled_states > 20 }
			}
			check_variable = { num_owned_controlled_states > 30 }
		}
	# }
}

is_secondary_power_eligible = {
	# custom_trigger_tooltip = {
	# 	tooltip = is_secondary_power_eligible_tt
		OR = {
			check_variable = { max_manpower_k > 12500 }
			AND = {
				check_variable = { max_manpower_k > 7500 }
				check_variable = { num_owned_controlled_states > 8 }
			}
			AND = {
				check_variable = { max_manpower_k > 5000 }
				check_variable = { num_owned_controlled_states > 10 }
			}
			check_variable = { num_owned_controlled_states > 15 }
		}
	# }
}

is_minor_power_eligible = {
	# custom_trigger_tooltip = {
	# 	tooltip = is_minor_power_eligible_tt
		OR = {
			check_variable = { max_manpower_k > 5000 }
			AND = {
				check_variable = { max_manpower_k > 2500 }
				check_variable = { num_owned_controlled_states > 5 }
			}
			AND = {
				check_variable = { max_manpower_k > 1750 }
				check_variable = { num_owned_controlled_states > 8 }
			}
			check_variable = { num_owned_controlled_states > 10 }
		}
	# }
}

is_irrelevant_power_eligible = {
	# custom_trigger_tooltip = {
	# 	tooltip = is_irrelevant_power_eligible_tt
		always = yes
	# }
}

toto_enabled_autonomy = {
	if = {
		limit = {
			OR = {
				has_dlc = "Together for Victory"
				has_dlc = "Death or Dishonor"
				has_dlc = "Waking the Tiger"
			}
		}
		always = yes
	}
	else = {
		always = no
	}
}