# Arability
  arability_modifier = {
    color_type = neutral
    value_type = number
    precision = 2
    category = country
    category = state
  }
  arability_factor = {
    color_type = neutral
    value_type = percentage
    precision = 1
    category = country
    category = state
  }
# Habitability
  habitability_modifier = {
    color_type = neutral
    value_type = number
    precision = 2
    category = country
    category = state
  }
  habitability_factor = {
    color_type = neutral
    value_type = percentage
    precision = 1
    category = country
    category = state
  }
# Sandstorm Chance
  sandstorm_chance_factor = {
    color_type = bad
    value_type = percentage
    precision = 1
    category = state
  }
# Drought Chance
  drought_chance_factor = {
    color_type = bad
    value_type = percentage
    precision = 1
    category = state
  }
# Heavy Rain Chance
  heavy_rain_chance_factor = {
    color_type = bad
    value_type = percentage
    precision = 1
    category = state
  }
# Monsoon Chance
  monsoon_chance_factor = {
    color_type = bad
    value_type = percentage
    precision = 1
    category = state
  }
# Flood Chance
  flood_chance_factor = {
    color_type = bad
    value_type = percentage
    precision = 1
    category = state
  }